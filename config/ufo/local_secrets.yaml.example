# Local secrets for UFO app credentials (keep this file untracked).
# Copy to config/ufo/local_secrets.yaml and fill in your values.

APP_CREDENTIALS:
  username: "your_email@example.com"
  password: "your_password_here"
